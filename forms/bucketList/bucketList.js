/*
let bucketList = []
let moreData = "yes"
let i = 0 

while(moreData == "yes") {
    let item = prompt("Enter a bucket list item")
    bucketList.push(item.toLowerCase());
    moreData = prompt("Enter yes to add more bucket list items")
    }

while (i < bucketList.length) {
    console.log(bucketList[i])
    i++
    }
*/